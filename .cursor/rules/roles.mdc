---
description: When changing Discord roles, consult and update the roles reference
globs: config/roles.js,commands/role-commands.js,commands/war-commands.js,commands/war-draft-commands.js,commands/reminder-commands.js,war-message-templates.js,events/role-events.js
alwaysApply: false
---

# Discord role changes

When **adding, renaming, or removing** any Discord role:

1. **Read `docs/ROLES.md` first.** It describes the single source of truth and touchpoints.
2. **Team roles:** Edit `config/roles.js` (TEAM_ROLES). Then update `war-message-templates.js` (strategy lines) and `commands/war-commands.js` (floor → roleSpecs) only if the tier affects those behaviors.
3. **Other roles (Forgetful, ShellShock):** Update the files listed in `docs/ROLES.md` for that role.
4. **Update `docs/ROLES.md`** if you add a new kind of role or change structure.

Use the checklists in `docs/ROLES.md` before finishing.
